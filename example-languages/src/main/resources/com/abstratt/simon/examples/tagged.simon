@language Simon
@import 'primitives'

package Tagged {
    objectTypes {
        [root] objectType Item {
            attributes {
                [name] attribute name { type: primitives.String }
                [multivalued] attribute tags { type: primitives.String }
            }
        }
    }
}
