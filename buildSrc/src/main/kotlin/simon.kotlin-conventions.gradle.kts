// For modules with Kotlin sources: the shared Java conventions plus the Kotlin JVM plugin.
// The Kotlin plugin version comes from the kotlin-gradle-plugin dependency declared in buildSrc.
plugins {
    id("simon.java-conventions")
    id("org.jetbrains.kotlin.jvm")
}
